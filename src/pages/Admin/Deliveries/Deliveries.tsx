import AdminPlaceholder from '../AdminPlaceholder';

const DeliveriesPage = () => (
    <AdminPlaceholder
        titleKey="pages.deliveries.title"
        descriptionKey="pages.deliveries.description"
    />
);

export default DeliveriesPage;
