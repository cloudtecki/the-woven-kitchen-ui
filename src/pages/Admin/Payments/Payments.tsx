import AdminPlaceholder from '../AdminPlaceholder';

const PaymentsPage = () => (
    <AdminPlaceholder
        titleKey="pages.payments.title"
        descriptionKey="pages.payments.description"
    />
);

export default PaymentsPage;
