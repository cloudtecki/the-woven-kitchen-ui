# Build-time environment variables (read by rspack via dotenv)
# Copy this file to .env / .env.development and set values.
REACT_APP_BUILD_NUMBER=

# Runtime configuration is injected via public/app-env.js (globalThis.twkVars):
#   apiUrl, apiScope, clientId, authority, baseUrl
